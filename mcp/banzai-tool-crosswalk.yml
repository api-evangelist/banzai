generated: '2026-08-06'
method: derived
source: mcp/banzai-mcp.yml + openapi/banzai-demio-openapi.yml
provider: Banzai
providerId: banzai
api: Public Demio API
note: >-
  Banzai publishes no MCP server and no GraphQL endpoint, so this crosswalk binds the
  CANDIDATE tool set in mcp/banzai-mcp.yml to the REST operations it was derived from.
  Every binding is one-to-one and high confidence because the tools were derived from
  the operations, not observed on a live server — there is no independent tool list to
  reconcile. Recorded so that a future run against a real Demio MCP server has a
  baseline to diff against.
surfaces:
  openapi:
    file: openapi/banzai-demio-openapi.yml
    origin: converted from the provider-published API Blueprint
    gated: false
  graphql:
    endpoint: null
    gated: null
  mcp:
    url: null
    gated: null
    status: none-published
crosswalk:
  - tool: ping_demio
    category: auth
    rest: [pingViaHeaders]
    binding: rest
    confidence: high
  - tool: list_events
    category: events
    rest: [listEvents]
    binding: rest
    confidence: high
  - tool: get_event
    category: events
    rest: [getEvent]
    binding: rest
    confidence: high
  - tool: get_event_session
    category: events
    rest: [getEventSession]
    binding: rest
    confidence: high
  - tool: register_for_event
    category: registration
    rest: [registerForEvent]
    binding: rest
    confidence: high
  - tool: list_session_participants
    category: reporting
    rest: [listSessionParticipants]
    binding: rest
    confidence: high
mcp_only: []
rest_only:
  - operation: pingViaQuery
    reason: >-
      Query-string variant of pingViaHeaders. Deliberately not exposed as a tool — it
      would put the API secret in a URL.
coverage:
  tools_named: 6
  tools_bound: 6
  mcp_only: 0
  rest_ops_total: 7
  rest_ops_with_tool: 6
